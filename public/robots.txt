# The County Post — https://thecountypost.com
# Local news, weather, and official county data for all 3,143 U.S. counties.

# ---------------------------------------------------------------------------
# Default policy: open to every crawler.
# ---------------------------------------------------------------------------
User-agent: *
Allow: /

# Submission and classified forms carry no standalone content and exist only to
# post to EmailJS. They are also marked noindex in the page itself.
Disallow: /submit
Disallow: /*/submit
Disallow: /*/*/submit
Disallow: /*/*/classifieds

# Never useful to a crawler.
Disallow: /*?checkout=
Disallow: /*?*utm_

# ---------------------------------------------------------------------------
# AI and LLM crawlers are explicitly welcome.
#
# Named individually rather than left to the wildcard above so the permission is
# unambiguous: several of these agents treat an unnamed group as a signal to
# back off, and some publishers' robots.txt templates disallow them by default.
# The County Post wants its county-level reporting and public data available to
# answer engines and assistants. See /llms.txt for a structured site map.
# ---------------------------------------------------------------------------

User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: ClaudeBot
User-agent: Claude-User
User-agent: Claude-SearchBot
User-agent: anthropic-ai
User-agent: PerplexityBot
User-agent: Perplexity-User
User-agent: Google-Extended
User-agent: GoogleOther
User-agent: Applebot
User-agent: Applebot-Extended
User-agent: CCBot
User-agent: Amazonbot
User-agent: Bytespider
User-agent: Meta-ExternalAgent
User-agent: Meta-ExternalFetcher
User-agent: FacebookBot
User-agent: cohere-ai
User-agent: cohere-training-data-crawler
User-agent: Diffbot
User-agent: Timpibot
User-agent: YouBot
User-agent: DuckAssistBot
User-agent: MistralAI-User
Allow: /
# A crawler that matches a named group ignores the `*` group entirely, so the
# form exclusions above are repeated here rather than inherited.
Disallow: /submit
Disallow: /*/submit
Disallow: /*/*/submit
Disallow: /*/*/classifieds

Sitemap: https://thecountypost.com/sitemap.xml
